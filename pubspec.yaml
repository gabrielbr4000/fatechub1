name: fatechub2
description: "Uma solução para a falta de comunicação."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  cloud_firestore: ^6.3.0
  device_preview_plus: ^2.6.1
  firebase_storage: 13.5.0
  firebase_auth: ^6.4.0
  firebase_core: ^4.7.0
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  record: ^5.0.0
  audioplayers: ^6.0.0
  path_provider: ^2.0.0
  web: ^1.0.0
  file_picker: ^9.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true

